---
name: kunonline-seo-daily
description: نظام النشر اليومي لمدونة كن أونلاين — ينشر مقالًا واحدًا فقط كل يوم من قائمة الـ100 مقال، بدورة عمل كاملة تشمل البحث والكتابة والتحقق وبوابة الجودة والنشر على GitHub. استخدمه عند التشغيل اليومي المجدول أو عند طلب "انشر مقال اليوم" أو "شغّل الدورة اليومية".
---

# نظام النشر اليومي — مدونة كن أونلاين

## القاعدة الحاكمة

**مقال واحد فقط كل يوم. لا أكثر — مهما حدث.**

الهدف: نشر 100 مقال تدريجيًا على مدى ~100 يوم. الجودة أهم من الجدول.

> `1 high-quality article per day` — وليس `1 article at any cost`

---

## المسارات

| العنصر | المسار |
|---|---|
| مستودع الموقع | `/root/kun-online-site` |
| خريطة المحتوى | `/root/.openclaw/workspace/skills/kunonline-seo-daily/seo-content-map.json` |
| ملف القفل | `/root/.openclaw/workspace/skills/kunonline-seo-daily/daily-publication-lock.json` |
| التقارير اليومية | `/root/.openclaw/workspace/skills/kunonline-seo-daily/reports/` |
| قالب المقالة | `/root/kun-online-site/blog/article-template.html` |
| فهرس المقالات | `/root/kun-online-site/blog/articles.json` |
| خريطة الموقع | `/root/kun-online-site/sitemap.xml` |
| صور المقالات | `/root/kun-online-site/blog/images/` |

---

# الخطوة 0 — فحص القفل (إلزامية قبل أي شيء)

اقرأ `daily-publication-lock.json` وقارن `date` بتاريخ اليوم (بتوقيت مصر).

**إذا `date` = اليوم و `status` = `published`:**

```
DAILY ARTICLE ALREADY PUBLISHED
```

**توقف فورًا. لا تكتب. لا تبحث. لا تنشر. أنهِ التشغيل.**

هذا يمنع النشر المزدوج عند: إعادة تشغيل OpenClaw، ريستارت الـVPS، تشغيل الكرون مرتين، أو تشغيل يدوي بالخطأ.

**إذا `date` = اليوم و `status` = `running`:**
يعني تشغيلًا سابقًا لم يكتمل. تحقق من `started_at` — إذا مضى أقل من ساعة، توقف (تشغيل متوازٍ). إذا مضى أكثر، اعتبره فاشلًا واستأنف نفس `article_id`.

**إذا `date` ≠ اليوم:** أكمل للخطوة 1.

---

# الخطوة 1 — تحديد المقال التالي

اقرأ `seo-content-map.json`.

**أولوية الاختيار:**
1. أي مقال حالته `needs_review` أو `failed` وقابل لإعادة المحاولة → عالجه أولًا
2. وإلا → أول مقال حالته `queued` بالترتيب الرقمي

**ممنوع Catch-Up:** إذا توقف النظام 4 أيام بعد المقال 17، فاليوم التالي هو المقال **18 فقط** — لا تنشر 4 مقالات للتعويض.

**اكتب القفل فورًا:**
```json
{"date":"2026-08-24","article_id":18,"status":"running","started_at":"2026-08-24T06:00:00Z","attempts":1}
```

---

# الخطوة 2 — البحث (Fresh Research Every Day)

لا تعتمد على بحث قديم. نفّذ البحث من جديد اليوم.

## 2.1 Keyword Research
- الكلمة المفتاحية الرئيسية وحجم بحثها التقريبي
- 5–10 كلمات ثانوية و Long-tail
- المصطلحات المرادفة (LSI)

## 2.2 Google Questions Research
اجمع من People Also Ask و Autocomplete و Related Searches:
- الأسئلة الفعلية التي يطرحها الناس حول الموضوع
- كل سؤال مهم = عنوان `<h2>` أو `<h3>` محتمل

## 2.3 SERP Analysis
افحص أول 10 نتائج:
- ما نوع المحتوى المتصدر؟ (دليل / مقارنة / قائمة / أداة)
- متوسط طول المقالات المتصدرة
- ما البنية التي يستخدمونها؟

## 2.4 Source Research
اجمع 3–5 مصادر موثوقة حديثة (تقارير رسمية، توثيق المنصات، دراسات).
انتبه لتاريخ المعلومة — خصوصًا في: SEO، Google، Meta Ads، TikTok Ads، Analytics، AI، Shopify، WooCommerce، أدوات التسويق.

## 2.5 Content Gap
حدد ما لم تغطه النتائج المتصدرة: سؤال بلا إجابة، معلومة قديمة، زاوية محلية (السوق المصري/الخليجي) مفقودة.

**هذه الفجوة هي سبب وجود مقالتك.**

## 2.6 تحسين العنوان (مسموح ومطلوب)

بناءً على نتائج البحث يمكنك تعديل: `H1`، `SEO Title`، `Secondary Keywords`، `Search Intent`، `Outline`، وزاوية المقال.

**لكن حافظ على الهدف التعليمي الأساسي وموقع المقال داخل الكتاب.** المقال رقم 23 عن التكلفة يظل عن التكلفة.

---

# الخطوة 3 — Content Brief

قبل الكتابة، جهّز:
- H1 النهائي و SEO Title (≤60 حرفًا)
- Meta Description (150–160 حرفًا)
- الكلمة الرئيسية + الثانوية
- Search Intent (informational / commercial / transactional)
- Outline كامل بعناوين `<h2>` و `<h3>`
- الفجوة التي ستملؤها
- الروابط الداخلية المخططة

---

# الخطوة 4 — كتابة المقال

**اللغة:** الفصحى المبسّطة — لا عامية، لأن الجمهور مصر + الخليج.

**الطول:** 1500–2200 كلمة. أقل من 1500 لا يجتاز بوابة الجودة.

**الأسلوب:**
- خاطب القارئ بـ "أنت"
- كل فقرة تحمل معلومة قابلة للتطبيق
- بلا حشو ولا مقدمات إنشائية
- أمثلة رقمية عملية حيثما أمكن

**الهيكل:**
1. مقدمة 100–150 كلمة بلا عنوان — تطرح المشكلة وتَعِد بالحل
2. 5–8 أقسام `<h2>` مبنية على الأسئلة الحقيقية من البحث
3. عناوين `<h3>` داخل الأقسام الطويلة
4. قائمة `<ul>` أو `<ol>` واحدة على الأقل — تساعد على Featured Snippets
5. `<div class="highlight-box">` واحد على الأقل لنصيحة أو تحذير
6. خاتمة `<h2>` تلخّص أهم 3 نقاط

**الوسوم المسموحة فقط:**
`<h2>` `<h3>` `<p>` `<ul>` `<ol>` `<li>` `<strong>` `<a>` `<blockquote>` `<div class="highlight-box"><h4>…</h4><p>…</p></div>`

ممنوع: `<h1>` (في القالب)، `<script>`، CSS مضمّن، `<img>` (الصورة تُدار عبر القالب).

---

# الخطوة 5 — Fact Check

راجع كل رقم وكل ادعاء:
- هل الرقم من مصدر موثوق؟ هل هو حديث؟
- هل اسم الأداة/الميزة ما زال صحيحًا؟
- هل السعر أو النسبة تغيّرت؟

**إذا لم تجد مصدرًا لرقم — احذفه بدل اختراعه.**

فشل هذه المرحلة = `needs_review`.

---

# الخطوة 6 — SEO Optimization

تحقق من:
- [ ] الكلمة الرئيسية في أول 100 كلمة
- [ ] الكلمة الرئيسية في `<h2>` واحد على الأقل
- [ ] الكلمة الرئيسية في الخاتمة
- [ ] كثافة طبيعية — لا حشو (إذا بدت جملة مصطنعة، أعد صياغتها)
- [ ] الكلمات الثانوية موزعة طبيعيًا
- [ ] SEO Title ≤ 60 حرفًا
- [ ] Meta Description 150–160 حرفًا وتنتهي بجملة كاملة
- [ ] الـslug قصير وواضح
- [ ] عناوين `<h2>` بصيغ قابلة للبحث

---

# الخطوة 7 — Internal Linking

**روابط خارجة من المقال الجديد:** 3–5 روابط لمقالات منشورة من `articles.json`، مرتبطة موضوعيًا، بنص وصفي (لا "اضغط هنا").

**روابط داخلة للمقال الجديد:** افتح 2–3 مقالات قديمة ذات صلة وأضف فيها رابطًا للمقال الجديد.

> تعديل روابط المقالات القديمة **لا يُعد مقالًا جديدًا** ومسموح في نفس التشغيل.

سجّل الروابط في `internal_links_out` و `internal_links_in` في خريطة المحتوى.

---

# الخطوة 8 — Featured Image

أنشئ صورة `1200×630` في `/root/kun-online-site/blog/images/{slug}.png`:
- خلفية بألوان الهوية: `#0a0f1e` مع لمسة `#7DC242`
- عنوان المقال بالعربية + شعار "كن أونلاين"

> **مهم:** استخدم خط `FreeSerifBold.ttf` فقط — هو الوحيد الذي يعرض العربية بشكل صحيح في Pillow. خطوط DejaVu و Poppins لا تعمل مع العربية.

استخدم `arabic_reshaper` + `python-bidi` لتشكيل النص قبل الرسم.

فشل إنشاء الصورة = `needs_review` — لا تنشر بلا صورة.

---

# الخطوة 9 — Quality Gate

**كل بند يجب أن يكون PASS. أي FAIL = لا نشر.**

| # | الفحص | معيار النجاح |
|---|---|---|
| 1 | البحث مكتمل | كل مراحل الخطوة 2 نُفّذت |
| 2 | الطول | ≥ 1500 كلمة |
| 3 | Fact Check | كل رقم له مصدر |
| 4 | البنية | مقدمة + 5 `<h2>`+ + قائمة + highlight-box + خاتمة |
| 5 | SEO | كل بنود الخطوة 6 محققة |
| 6 | الروابط الداخلية | ≥ 3 روابط خارجة |
| 7 | الصورة | موجودة وبالمقاس الصحيح |
| 8 | HTML | كل الوسوم مغلقة، لا وسوم ممنوعة |
| 9 | الأصالة | لا نسخ، صياغة أصلية بالكامل |
| 10 | عدم التكرار | لا يكرر مقالًا منشورًا (تحقق من `articles.json`) |

**عند الفشل:** غيّر الحالة إلى `needs_review` أو `failed`، سجّل السبب في `failure_reason`، حدّث القفل بـ `status: failed`، واجعل نفس المقال أول أولوية غدًا. **لا تنتقل للمقال التالي.**

---

# الخطوة 10 — النشر

1. املأ قالب `article-template.html` واحفظ في `/root/kun-online-site/blog/{slug}.html`
2. أضف الكائن في **بداية** `articles.json`
3. أعد بناء `sitemap.xml` كاملًا (4 صفحات ثابتة + كل المقالات)
4. احفظ تعديلات الروابط الداخلية في المقالات القديمة
5. ارفع:

```bash
cd /root/kun-online-site
git add -A
git commit -m "مقال {n}/100: {title}"
git push origin main
```

Cloudflare Pages ينشر خلال ~دقيقة.

**إذا فشل الـpush:** لا تحذف الملفات. سجّل الخطأ، اترك التغييرات محلية، وأبلغ أحمد.

---

# الخطوة 11 — Post-Publish QA

انتظر دقيقتين ثم تحقق:
- [ ] `https://www.kun-online.com/blog/{slug}` يفتح بلا 404
- [ ] المقال يظهر في `/blog`
- [ ] الصورة تظهر
- [ ] الروابط الداخلية تعمل
- [ ] `sitemap.xml` يحتوي الرابط الجديد

---

# الخطوة 12 — تحديث الحالة

**في `seo-content-map.json`:**
```json
{"status":"published","published_date":"2026-08-24","slug":"...","url":"...","word_count":1680,
 "final_h1":"...","primary_keyword":"...","search_intent":"..."}
```

**في `progress`:**
`articles_published` +1 · `articles_remaining` −1 · `next_article` · `project_completion_percentage` · `last_published_date`

**في `daily-publication-lock.json`:**
```json
{"date":"2026-08-24","article_id":18,"status":"published","published_at":"2026-08-24T06:42:00Z"}
```

---

# الخطوة 13 — التقرير اليومي

احفظ في `reports/{YYYY-MM-DD}.md` وأرسله على واتساب:

```
## DAILY SEO CONTENT REPORT

**Date:** 2026-08-24
**Article Number:** 18 / 100
**Article:** طرق البحث عن المنتجات الرابحة دون الوقوع في فخ الترند
**Primary Keyword:** المنتجات الرابحة
**Search Intent:** Informational

**Research Completed:** PASS
**Content Quality:** PASS
**Featured Image:** PASS
**Internal Linking:** PASS
**SEO QA:** PASS

**Published URL:** https://www.kun-online.com/blog/...
**Publication Status:** PUBLISHED

**Total Published:** 18 / 100
**Next Article:** 19
```

---

# الخطوة 14 — التوقف

```
STOP UNTIL NEXT DAILY RUN
```

**لا تبدأ مقالًا آخر اليوم مهما كان السبب.**

---

# حالة اكتمال المشروع

بعد نشر المقال **100** بنجاح:

```json
{"project": {"status": "CONTENT_PROJECT_COMPLETE"}}
```

**أوقف إنشاء مقالات جديدة. لا تبدأ من المقال 1 مرة أخرى.**

المرحلة التالية `CONTENT OPTIMIZATION & REFRESH` (مراجعة الأداء، تحديث المقالات، تحسين CTR، معالجة Cannibalization، إضافة روابط داخلية) **تبدأ بأمر صريح من أحمد فقط** — ولا تُنشئ مقالات خارج قائمة الـ100.

---

# ملخص القاعدة النهائية

> **Research deeply → Produce one excellent article → Optimize → Create image → Publish → QA → Stop.**
